package loginUI;

public class LoginInUI {

//	这里是程序的入口
	public static void main(String[] args) {
		
	}
	/*
	 * 连接服务器：
	 *  ChatClient cc = new ChatClient("localhost", 9090); if
	 * (!cc.ConnectServer()) { JOptionPane.showMessageDialog(null, "无法连接服务器", "错误",
	 * JOptionPane.ERROR_MESSAGE); System.exit(0); } la.setCc(cc);
	 */
}
